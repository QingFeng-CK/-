数据输入
Scanner使用的基本步骤：
1 导包：
import java.util.Scanner;
注意：导包的动作必须出现在public class 类名 {
}的上面
2 创建对象
Scanner sc = new Scanner(System.in);
注意：上面这个格式里面，只有sc是变量名，可以变，其他的不可以变。如果sc改变了，接收数据里面的sc也要跟着改变
3 接受数据
int i = sc.mextlnt ();
注意：上面这个格式里面，只有i是变量名，可以变，其他的不可以变